module groundwater;

interface SystemCalls {
    [Sync]
    Socket() => (int64 fd);
    [Sync]
    Open(string filepath) => (int64 fd);
    [Sync]
    Close(int64 fd) => ();
    [Sync]
    Read(int64 fd, int64 numBytes) => (array<uint8> bytes);
};
